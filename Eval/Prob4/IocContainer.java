package Eval.Prob4;

public class IocContainer {
    //while making the object if we are using "new" operator the control is in pur hand but if we are using Externel Entity to
    //inject this object then it is called inversion of control.With the help of Spring Container(Externel Entity) We can
    // achieve this or through design patterns
}
